<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.MessageTypeMapper">

    <select id="list" resultType="MessageType">
        select messageType.*, userInfo.userName from `PMS_MessageType` messageType, `PMS_UserInfo` userInfo
        where messageType.userId = userInfo.id
    </select>
</mapper>
