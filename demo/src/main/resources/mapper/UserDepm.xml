<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.UserDepmMapper">


<!--    <update id="updateOpenIdAndNickName" parameterType="User" >-->
<!--        update userInfo set openid=#{openId}, wxnickname = #{wxNickname} where id=#{id}-->
<!--    </update>-->

    <select id="list" resultType="UserDepm">
        select * from dbo.sts_UserDepm
    </select>

</mapper>
